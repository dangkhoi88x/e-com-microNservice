import { useEffect, useMemo, useState } from "react";
import { Link, useNavigate, useSearchParams } from "react-router-dom";
import { Filter, Search, SlidersHorizontal, Star } from "lucide-react";
import { getCategories } from "../services/categoryService";
import { getProductAggregations, searchProductSuggestions, searchProducts } from "../services/productService";
import "./ShopSearch.css";

const money = (value) => new Intl.NumberFormat("vi-VN", { style: "currency", currency: "VND", maximumFractionDigits: 0 }).format(value || 0);
const slugify = (value) => (value || "san-pham").toLocaleLowerCase("vi-VN").normalize("NFD").replace(/[\u0300-\u036f]/g, "").replace(/đ/g, "d").replace(/[^a-z0-9]+/g, "-").replace(/^-|-$/g, "");
const image = (item) => item.thumbnailUrl || item.images?.find((entry) => entry.isPrimary)?.url || item.images?.[0]?.url;

export default function ShopSearch() {
  const navigate = useNavigate(); const [params, setParams] = useSearchParams();
  const [query, setQuery] = useState(params.get("q") || ""); const [products, setProducts] = useState([]); const [categories, setCategories] = useState([]); const [suggestions, setSuggestions] = useState([]); const [aggregation, setAggregation] = useState({ categories: [] }); const [loading, setLoading] = useState(false);
  const filters = useMemo(() => ({ q: params.get("q") || undefined, categoryId: params.get("categoryId") || undefined, minPrice: params.get("minPrice") || undefined, maxPrice: params.get("maxPrice") || undefined, inStock: params.get("inStock") === "true" ? true : undefined, sort: params.get("sort") || "createdAt,desc", page: 1, size: 24 }), [params]);
  useEffect(() => { getCategories().then(setCategories).catch(() => {}); }, []);
  useEffect(() => { setLoading(true); Promise.all([searchProducts(filters), getProductAggregations(filters)]).then(([data, facets]) => { setProducts(data.content || []); setAggregation(facets); }).catch(() => { setProducts([]); }).finally(() => setLoading(false)); }, [filters]);
  useEffect(() => { const timer = window.setTimeout(() => searchProductSuggestions(query).then(setSuggestions).catch(() => setSuggestions([])), 300); return () => window.clearTimeout(timer); }, [query]);
  const setFilter = (key, value) => { const next = new URLSearchParams(params); value === undefined || value === "" || value === false ? next.delete(key) : next.set(key, value); setParams(next); };
  const submit = (event) => { event.preventDefault(); setFilter("q", query); setSuggestions([]); };
  return <main className="shop-search-page"><header><Link to="/shop">Nova<span>Shop</span></Link><form onSubmit={submit}><Search size={18}/><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Tìm sản phẩm, thương hiệu..." />{suggestions.length > 0 && <div className="shop-search-suggestions">{suggestions.map((item) => <button type="button" key={item.productId} onClick={() => { setQuery(item.name); setFilter("q", item.name); setSuggestions([]); }}><img src={image(item)} alt=""/><span>{item.name}<small>{money(item.price)}</small></span></button>)}</div>}</form><Link to="/cart">Giỏ hàng</Link></header><section className="shop-search-shell"><aside><div className="shop-search-filter-title"><Filter size={17}/><h2>Bộ lọc</h2></div><b>Danh mục</b>{categories.map((category) => <label key={category.id}><input type="radio" checked={filters.categoryId === category.id} onChange={() => setFilter("categoryId", category.id)} /> {category.name}</label>)}<button onClick={() => setFilter("categoryId", "")}>Tất cả danh mục</button><b>Khoảng giá</b><div className="shop-search-prices"><input value={params.get("minPrice") || ""} onChange={(event) => setFilter("minPrice", event.target.value)} placeholder="Từ" type="number"/><input value={params.get("maxPrice") || ""} onChange={(event) => setFilter("maxPrice", event.target.value)} placeholder="Đến" type="number"/></div><b>Tình trạng</b><label><input type="checkbox" checked={filters.inStock === true} onChange={(event) => setFilter("inStock", event.target.checked ? "true" : "")} /> Còn hàng</label><button className="shop-search-reset" onClick={() => { setQuery(""); setParams({}); }}>Xóa bộ lọc</button></aside><section><div className="shop-search-head"><div><p>TÌM KIẾM SẢN PHẨM</p><h1>{filters.q ? `Kết quả cho “${filters.q}”` : "Khám phá sản phẩm"}</h1><small>{products.length} sản phẩm phù hợp</small></div><label><SlidersHorizontal size={16}/><select value={filters.sort} onChange={(event) => setFilter("sort", event.target.value)}><option value="createdAt,desc">Mới nhất</option><option value="price,asc">Giá thấp đến cao</option><option value="price,desc">Giá cao đến thấp</option></select></label></div><div className="shop-search-chips">{aggregation.categories?.slice(0, 6).map((item) => <span key={item.name}>{item.name} · {item.count}</span>)}</div>{loading ? <div className="shop-search-empty">Đang tìm sản phẩm…</div> : <div className="shop-search-grid">{products.map((item) => <article key={item.productId} onClick={() => navigate(`/shop/products/${slugify(item.name)}-${item.productId}`)}>{image(item) ? <img src={image(item)} alt={item.name}/> : <Search size={38}/>}<div><small>{item.categoryName}</small><h2>{item.name}</h2><p><Star size={13} fill="currentColor"/> 4.8</p><strong>{money(item.price)}</strong></div></article>)}</div>}{!loading && !products.length && <div className="shop-search-empty">Không tìm thấy sản phẩm phù hợp. Hãy thay đổi từ khóa hoặc bộ lọc.</div>}</section></section></main>;
}
