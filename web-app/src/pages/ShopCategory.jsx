import { useEffect, useMemo, useState } from "react";
import { Link, useNavigate, useParams } from "react-router-dom";
import { ChevronDown, Filter, Heart, Search, ShoppingBag, SlidersHorizontal, Star, Truck } from "lucide-react";
import { getCategories } from "../services/categoryService";
import { getProductAggregations, searchProducts } from "../services/productService";
import "./ShopCategory.css";

const slugify = (value) => (value || "danh-muc").toLocaleLowerCase("vi-VN").normalize("NFD").replace(/[\u0300-\u036f]/g, "").replace(/đ/g, "d").replace(/[^a-z0-9]+/g, "-").replace(/^-|-$/g, "");
const idFromSlug = (slug) => slug?.match(/[0-9a-f]{8}-(?:[0-9a-f]{4}-){3}[0-9a-f]{12}$/i)?.[0] || slug;
const categoryUrl = (item) => `/shop/categories/${slugify(item.name)}-${item.id}`;
const productUrl = (item) => `/shop/products/${slugify(item.name)}-${item.productId || item.id}`;
const money = (value) => new Intl.NumberFormat("vi-VN", { style: "currency", currency: "VND", maximumFractionDigits: 0 }).format(value || 0);
const image = (item) => item.thumbnailUrl || item.images?.find((entry) => entry.isPrimary)?.url || item.images?.[0]?.url;

export default function ShopCategory() {
  const { slug } = useParams(); const navigate = useNavigate(); const categoryId = idFromSlug(slug);
  const [categories, setCategories] = useState([]); const [products, setProducts] = useState([]); const [total, setTotal] = useState(0); const [facets, setFacets] = useState({ priceStats: {} });
  const [query, setQuery] = useState(""); const [sort, setSort] = useState("createdAt,desc"); const [minPrice, setMinPrice] = useState(""); const [maxPrice, setMaxPrice] = useState(""); const [inStock, setInStock] = useState(false); const [loading, setLoading] = useState(false);
  useEffect(() => { getCategories().then(setCategories).catch(() => {}); }, []);
  const searchParams = useMemo(() => ({ page: 1, size: 60, q: query || undefined, categoryId, minPrice: minPrice || undefined, maxPrice: maxPrice || undefined, inStock: inStock || undefined, sort }), [query, categoryId, minPrice, maxPrice, inStock, sort]);
  useEffect(() => { const timer = window.setTimeout(() => { setLoading(true); Promise.all([searchProducts(searchParams), getProductAggregations(searchParams)]).then(([result, aggregation]) => { setProducts(result.content || []); setTotal(result.totalElements || 0); setFacets(aggregation || { priceStats: {} }); }).catch(() => { setProducts([]); setTotal(0); }).finally(() => setLoading(false)); }, 250); return () => window.clearTimeout(timer); }, [searchParams]);
  const category = categories.find((item) => item.id === categoryId); const reset = () => { setQuery(""); setMinPrice(""); setMaxPrice(""); setInStock(false); setSort("createdAt,desc"); };
  return <main className="category-page"><header className="category-topbar"><Link to="/shop" className="category-brand"><i>N</i>Nova<span>Shop</span></Link><label><Search size={17}/><input placeholder="Tìm trong danh mục này" value={query} onChange={(event) => setQuery(event.target.value)}/></label><Link to="/cart"><ShoppingBag size={18}/> Giỏ hàng</Link></header><section className="category-hero"><div><p>KHÁM PHÁ BỘ SƯU TẬP</p><h1>{category?.name || "Danh mục sản phẩm"}</h1><span>{total} sản phẩm phù hợp với bộ lọc của bạn</span></div><ShoppingBag size={88}/></section><section className="category-shell"><aside className="category-sidebar"><div className="category-sidebar-title"><h2>Danh mục</h2><ChevronDown size={17}/></div><nav>{categories.map((item) => <Link key={item.id} className={item.id === categoryId ? "active" : ""} to={categoryUrl(item)}>{item.name}</Link>)}</nav><div className="category-filter-title"><Filter size={17}/><h2>Bộ lọc</h2></div><label className="category-search"><Search size={15}/><input placeholder="Tên sản phẩm" value={query} onChange={(event) => setQuery(event.target.value)}/></label><div className="category-filter"><b>Khoảng giá</b><div><input placeholder="Từ" value={minPrice} onChange={(event) => setMinPrice(event.target.value)} type="number"/><i>—</i><input placeholder="Đến" value={maxPrice} onChange={(event) => setMaxPrice(event.target.value)} type="number"/></div>{facets.priceStats?.min && <small>{money(facets.priceStats.min)} – {money(facets.priceStats.max)}</small>}</div><div className="category-filter"><b>Tình trạng</b><label><input type="checkbox" checked={inStock} onChange={(event) => setInStock(event.target.checked)}/> Chỉ hiện sản phẩm còn hàng</label></div><button className="category-reset" onClick={reset}>Xóa bộ lọc</button></aside><div className="category-results"><div className="category-results-head"><div><p>HIỂN THỊ {total} SẢN PHẨM</p><h2>{category?.name || "Sản phẩm"}</h2></div><label className="category-sort"><SlidersHorizontal size={16}/><select value={sort} onChange={(event) => setSort(event.target.value)}><option value="createdAt,desc">Mới nhất</option><option value="price,asc">Giá thấp đến cao</option><option value="price,desc">Giá cao đến thấp</option></select></label></div><div className="category-grid">{products.map((item, index) => <article key={item.productId} onClick={() => navigate(productUrl(item))}><div className="category-product-image">{index < 3 && <b>−{10 + index * 5}%</b>}<button onClick={(event) => event.stopPropagation()}><Heart size={17}/></button>{image(item) ? <img src={image(item)} alt={item.name}/> : <ShoppingBag size={35}/>}</div><div><p>{item.categoryName || category?.name}</p><h3>{item.name}</h3><strong>{money(item.price)}</strong><span><Star size={13} fill="currentColor"/> 4.8 · Đã bán 120</span></div></article>)}</div>{loading && <div className="category-empty">Đang áp dụng bộ lọc…</div>}{!loading && !products.length && <div className="category-empty"><ShoppingBag size={38}/><h3>Chưa có sản phẩm phù hợp</h3><p>Hãy thử xóa bộ lọc hoặc chọn danh mục khác.</p><button onClick={reset}>Xem lại sản phẩm</button></div>}<div className="category-services"><div><Truck size={22}/><p><b>Giao hàng nhanh</b>Đến tận tay trong 2–5 ngày</p></div><div><Heart size={21}/><p><b>Chọn lọc kỹ lưỡng</b>Chất lượng là ưu tiên hàng đầu</p></div></div></div></section></main>;
}
