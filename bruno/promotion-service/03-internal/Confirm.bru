meta {
  name: Confirm
  type: http
  seq: 4
}

post {
  url: {{promotionUrl}}/internal/promotions/confirm
  body: json
}

headers {
  content-type: application/json
}

body:json {
  {
    "orderId": "{{confirmOrderId}}"
  }
}
