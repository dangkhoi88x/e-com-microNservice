meta {
  name: Release
  type: http
  seq: 3
}

post {
  url: {{promotionUrl}}/internal/promotions/release
  body: json
}

headers {
  content-type: application/json
}

body:json {
  {
    "orderId": "{{releaseOrderId}}"
  }
}
