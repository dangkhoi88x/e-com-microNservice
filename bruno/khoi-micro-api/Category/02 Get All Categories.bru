meta {
  name: 02 Get All Categories
  type: http
  seq: 2
}

get {
  url: {{productBaseUrl}}/api/v1/categories
  body: none
  auth: none
}

tests {
  test("status is 200 OK", function () {
    expect(res.status).to.equal(200);
  });

  test("data is an array", function () {
    expect(res.body.data).to.be.an("array");
  });
}
